{
    "tasks": [
        {
            "type": "shell",
            "label": "ProWo build debug version",
            "command": "/usr/bin/make -f ${workspaceRoot}/Makefile CONF=Debug",
            "args": [],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        },
        {
            "type": "shell",
            "label": "ProWo build release version",
            "command": "/usr/bin/make -f ${workspaceRoot}/Makefile CONF=Release",
            "args": [

            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "compiler: /usr/bin/g++"
        },
        {
            "type": "shell",
            "label": "ProWo clean",
            "command": "rm -f -r ${workspaceRoot}/build",
            "group": "clean",
            "detail": "clean directory build"
        }        
    ],
    "version": "2.0.0"
}